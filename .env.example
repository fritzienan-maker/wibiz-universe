# ─── App ──────────────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
APP_BASE_URL=https://universe.yourdomain.com

# ─── Database ─────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://user:password@host:5432/universe_db

# ─── Auth ─────────────────────────────────────────────────────────────────────
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change-this-to-a-long-random-string-at-least-32-chars
JWT_EXPIRES_IN=7d

# ─── Admin seed (used ONCE by: npx tsx scripts/seed-admin.ts) ─────────────────
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=change-before-seeding

# ─── GHL (GoHighLevel) ────────────────────────────────────────────────────────
# ROTATE IMMEDIATELY if these values were ever committed or shared
GHL_API_KEY=pit-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
GHL_LOCATION_ID=your-ghl-location-id
GHL_WEBHOOK_SECRET=change-this-to-a-shared-secret
# Custom field ID written with temp password on provisioning
GHL_TEMP_PASS_FIELD_ID=your-field-id
# Custom field ID written with staff invite URL (optional)
GHL_STAFF_INVITE_FIELD_ID=
# GHL inbound webhook URL to forward support tickets (optional)
GHL_SUPPORT_WEBHOOK_URL=

# ─── DocuSeal (formal document signing) ──────────────────────────────────────
DOCUSEAL_API_TOKEN=your-docuseal-api-token-here

# ─── Cloudinary ──────────────────────────────────────────────────────────────
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
VITE_CLOUDINARY_UPLOAD_PRESET=universe_app

# ─── Phase 2+ (leave blank) ───────────────────────────────────────────────────
# JOTFORM_WEBHOOK_SECRET=
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# S3_BUCKET=
# S3_REGION=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=